# TODO

- [ ] Inspect existing UI patterns for products listing (grid, CTAs, any inputs)
- [x] Add skeleton loading UI for `/products` grid (image/title/spec bullets/CTA)
- [x] Add search input + scrollable results area on `/products`

- [ ] Prioritize important copy by mixing red within sentences (small targeted copy updates on `/products`)
- [ ] Make CTA/button text clearer and more actionable on `/products`
- [ ] Reduce color saturation in dark mode by adding `dark:` variants where needed on `/products`
- [ ] Standardize gaps within the `/products` grid + sections (consistent `gap-*`)
- [ ] Test in dev server: verify skeleton appears, search works, scroll behaves as expected

